[defaults]
inventory = inventory.yml
roles_path = roles
# Secrets never appear in output; tasks that write them also set no_log/diff.
display_skipped_hosts = false
interpreter_python = auto_silent

[privilege_escalation]
become = true
